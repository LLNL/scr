LIST(APPEND liber_srcs
    er.c
    er_util.c
)

ADD_LIBRARY(er_o OBJECT $<TARGET_OBJECTS:shuffile_o> $<TARGET_OBJECTS:kvtree_o> $<TARGET_OBJECTS:redset_o> ${liber_srcs})

IF(BUILD_SHARED_LIBS)
  SET_PROPERTY(TARGET er_o PROPERTY POSITION_INDEPENDENT_CODE 1)
ENDIF()

TARGET_COMPILE_DEFINITIONS(er_o PRIVATE REDSET_ENABLE_MPI)
