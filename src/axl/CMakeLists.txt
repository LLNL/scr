LIST(APPEND libaxl_srcs
    axl.c
    axl_sync.c
    axl_err.c
    axl_io.c
    axl_util.c
)

# PTHREADS
IF(ENABLE_PTHREADS)
  FIND_PACKAGE(Threads REQUIRED)

  IF(CMAKE_USE_PTHREADS_INIT)
      SET(HAVE_PTHREADS TRUE)
      # TARGET_COMPILE_DEFINITIONS(axl_o PRIVATE HAVE_PTHREADS)
      # TARGET_COMPILE_DEFINITIONS(axl_noMPI_o PRIVATE HAVE_PTHREADS)
  ENDIF()

  LIST(APPEND libaxl_srcs axl_pthread.c)
ENDIF(ENABLE_PTHREADS)


IF(BBAPI_FOUND)
    LIST(APPEND libaxl_srcs axl_async_bbapi.c)
ENDIF(BBAPI_FOUND)

IF(HAVE_DATAWARP)
    LIST(APPEND libaxl_srcs axl_async_datawarp.c)
ENDIF(HAVE_DATAWARP)

## DataMover
IF(ENABLE_HPE_NNFDM)
  FIND_PACKAGE(NNFDM REQUIRED)
  IF(NNFDM_FOUND)
    SET(HAVE_NNFDM TRUE)
    INCLUDE_DIRECTORIES(${NNFDM_INCLUDE_DIR})
    MESSAGE(STATUS "NNFDM_INCLUDE_DIR: ${NNFDM_INCLUDE_DIR}")
    MESSAGE(STATUS "NNFDM_LIBRARIES: ${NNFDM_LIBRARIES}")
    LIST(APPEND AXL_EXTERNAL_LIBS ${NNFDM_LIBRARIES})
    LIST(APPEND AXL_EXTERNAL_STATIC_LIBS ${NNFDM_LIBRARIES})
  ENDIF(NNFDM_FOUND)
ENDIF(ENABLE_HPE_NNFDM)

IF(HAVE_NNFDM)
    LIST(APPEND libaxl_srcs nnfdm.cpp)
ENDIF(HAVE_NNFDM)

IF(MPI AND MPI_FOUND)
  LIST(APPEND libaxl_srcs axl_mpi.c)
ENDIF(MPI AND MPI_FOUND)

ADD_LIBRARY(axl_o OBJECT ${libaxl_srcs})
ADD_LIBRARY(axl_noMPI_o OBJECT ${libaxl_srcs})

